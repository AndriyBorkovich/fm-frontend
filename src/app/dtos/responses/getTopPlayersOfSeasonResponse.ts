export interface TopScorer {
  playerId: number,
  name: string;
  clubName: string;
  goalsCount: number;
}

export interface TopAssistant {
  playerId: number,
  name: string;
  clubName: string;
  assistsCount: number;
}